export const Path = ({pathTitle}) => {
    return (
        <div className="bg-grayDarker py-6 font-sans">
            <h1 className="uppercase text-zinc-100 uppercase font-medium text-3xl container mx-auto">{pathTitle}</h1>
        </div>
    );
};
